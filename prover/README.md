# Prover Module

This module initializes the Go-based ZK Prover service.

- Entry point: `cmd/prover/main.go`
- Internal package: `internal/prover`
- Go version: 1.22

Build:

```bash
cd prover
go build ./cmd/prover
```

Run:

```bash
cd prover
go run ./cmd/prover
```

